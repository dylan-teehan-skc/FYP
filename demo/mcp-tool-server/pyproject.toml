[project]
name = "mcp-tool-server"
version = "0.1.0"
description = "MCP server exposing tools for agents"
requires-python = ">=3.11"

dependencies = [
    "fastapi>=0.104.1",
    "uvicorn>=0.24.0",
    "pydantic>=2.5.0",
]

[project.scripts]
mcp-server = "main:run"
